# Proxy para desarrollo local (ng serve)
# En producción/Docker el front usa enap_api: '/api' y nginx hace el proxy.

{
  "/api": {
    "target": "http://localhost:3000",
    "secure": false,
    "changeOrigin": true,
    "logLevel": "debug",
    "pathRewrite": {
      "^/api": ""
    }
  }
}
